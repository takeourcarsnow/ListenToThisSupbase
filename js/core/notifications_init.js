import notificationsView from '../views/notifications_view.js';

// Initialize notifications UI on DOMContentLoaded
window.addEventListener('DOMContentLoaded', function() {
  notificationsView.init();
});
